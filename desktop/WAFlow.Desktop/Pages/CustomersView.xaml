<UserControl x:Class="WAFlow.Desktop.Pages.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,26,28,28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="客户列表" Style="{StaticResource PageTitle}"/><TextBlock Text="核心字段 + 上传表格自定义维度；不同客户可以拥有不同字段。" Foreground="{StaticResource Muted}" Margin="0,5,0,0"/></StackPanel>
            <Button Grid.Column="1" Content="＋ 导入客户" Style="{StaticResource PrimaryButton}" Click="Import_Click" VerticalAlignment="Center"/>
        </Grid>
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,20,0,14">
            <WrapPanel>
                <TextBox x:Name="SearchBox" Width="210" ToolTip="搜索所有字段（含自定义维度）" Margin="0,0,10,8" KeyDown="SearchBox_KeyDown"/>
                <ComboBox x:Name="GradeFilter" Width="105" Margin="0,0,10,8" SelectionChanged="Filter_SelectionChanged"/>
                <ComboBox x:Name="StageFilter" Width="125" Margin="0,0,10,8" DisplayMemberPath="Label" SelectionChanged="Filter_SelectionChanged"/>
                <TextBox x:Name="TagFilterBox" Width="120" ToolTip="标签包含" Margin="0,0,10,8" KeyDown="SearchBox_KeyDown"/>
                <TextBox x:Name="OwnerFilterBox" Width="130" ToolTip="负责人包含" Margin="0,0,10,8" KeyDown="SearchBox_KeyDown"/>
                <ComboBox x:Name="CustomFieldFilter" Width="165" ToolTip="选择自定义维度" Margin="0,0,10,8" SelectionChanged="Filter_SelectionChanged"/>
                <TextBox x:Name="CustomValueFilterBox" Width="145" ToolTip="自定义维度值包含" Margin="0,0,10,8" KeyDown="SearchBox_KeyDown"/>
                <Button Content="清除筛选" Style="{StaticResource SecondaryButton}" Click="Clear_Click" Margin="0,0,0,8"/>
            </WrapPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="CustomerGrid" IsReadOnly="True" EnableRowVirtualization="True" EnableColumnVirtualization="True" ScrollViewer.CanContentScroll="True" FrozenColumnCount="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="客户" Binding="{Binding DisplayName}" Width="1.1*"/>
                    <DataGridTextColumn Header="公司" Binding="{Binding Company}" Width="1.3*"/>
                    <DataGridTextColumn Header="国家 / 地区" Binding="{Binding Country}" Width="*"/>
                    <DataGridTextColumn Header="WhatsApp" Binding="{Binding PhoneE164}" Width="1.05*"/>
                    <DataGridTextColumn Header="号码" Binding="{Binding PhoneState}" Width="65"/>
                    <DataGridTextColumn Header="标签" Binding="{Binding TagsLabel}" Width="1.1*"/>
                    <DataGridTextColumn Header="负责人" Binding="{Binding Owner}" Width="*"/>
                    <DataGridTextColumn Header="等级" Binding="{Binding Grade}" Width="60"/>
                    <DataGridTextColumn Header="阶段" Binding="{Binding StageLabel}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
