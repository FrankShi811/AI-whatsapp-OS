<Window x:Class="WAFlow.Desktop.Windows.VersionHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Sales OS · 版本更新历史" Width="760" Height="700" MinWidth="620" MinHeight="520"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" ShowInTaskbar="False"
        Icon="/AISalesOS;component/Assets/AI-Sales-OS.ico">
    <Grid Background="{DynamicResource Canvas}">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Background="{DynamicResource SurfaceElevated}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Padding="28,22">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="PRODUCT RELEASES" Style="{StaticResource PageEyebrow}"/>
                    <TextBlock Text="版本更新历史" FontSize="25" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="查看当前版本以及已交付的重要功能变化。" Foreground="{DynamicResource Muted}" Margin="0,5,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="{DynamicResource PrimarySoft}" CornerRadius="14" Padding="14,8" VerticalAlignment="Center">
                    <TextBlock x:Name="CurrentVersionText" Foreground="{DynamicResource PrimaryDark}" FontWeight="Bold"/>
                </Border>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28,22,28,10">
                <Border Style="{StaticResource AiCard}" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="GITHUB RELEASE UPDATE" Style="{StaticResource PageEyebrow}"/>
                                <TextBlock Text="应用更新" FontSize="18" FontWeight="SemiBold" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" x:Name="UpdateStatusBadge" Background="{DynamicResource PrimarySoft}" CornerRadius="12" Padding="11,6" VerticalAlignment="Center">
                                <TextBlock x:Name="UpdateStatusBadgeText" Text="准备检查" Foreground="{DynamicResource PrimaryDark}" FontWeight="SemiBold"/>
                            </Border>
                        </Grid>
                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="当前版本" Foreground="{DynamicResource Muted}" FontSize="10.5"/>
                                <TextBlock x:Name="InstalledVersionValue" FontSize="20" FontWeight="Bold" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="最新版本" Foreground="{DynamicResource Muted}" FontSize="10.5"/>
                                <TextBlock x:Name="LatestVersionValue" Text="检查后显示" FontSize="20" FontWeight="Bold" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock x:Name="UpdateStatusText" Text="启动后会自动检查 GitHub Release。" Foreground="{DynamicResource InkSecondary}" Margin="0,15,0,0" TextWrapping="Wrap"/>
                        <ProgressBar x:Name="UpdateProgress" Height="6" Minimum="0" Maximum="100" Margin="0,12,0,0" Visibility="Collapsed"/>
                        <Border x:Name="ReleaseNotesPanel" Background="{DynamicResource Surface}" CornerRadius="10" BorderBrush="{DynamicResource Line}" BorderThickness="1" Padding="13" Margin="0,13,0,0" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="更新日志" FontWeight="SemiBold"/>
                                <TextBlock x:Name="RemoteReleaseNotesText" Foreground="{DynamicResource InkSecondary}" Margin="0,7,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button x:Name="CheckUpdateButton" Content="重新检查" Style="{StaticResource SecondaryButton}" Click="CheckUpdate_Click" Margin="0,0,8,0"/>
                            <Button x:Name="InstallUpdateButton" Content="安装更新并重启" Style="{StaticResource PrimaryButton}" Click="InstallUpdate_Click" IsEnabled="False"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <TextBlock Text="版本更新历史" Style="{StaticResource SectionTitle}" Margin="2,0,0,12"/>
                <ItemsControl x:Name="ReleaseList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,14" Padding="20">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="88"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <Border Background="{DynamicResource AiSoft}" CornerRadius="10" Padding="9,6" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding VersionLabel}" Foreground="{DynamicResource AiAccentDeep}" FontWeight="Bold"/>
                                    </Border>
                                    <TextBlock Text="{Binding Date}" Foreground="{DynamicResource Muted}" FontSize="10" Margin="1,7,0,0"/>
                                    <Border Background="{DynamicResource SuccessSoft}" CornerRadius="8" Padding="7,4" Margin="0,8,0,0" HorizontalAlignment="Left" Visibility="{Binding CurrentVisibility}">
                                        <TextBlock Text="当前版本" Foreground="{DynamicResource Success}" FontSize="9" FontWeight="SemiBold"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{Binding Changes}" Margin="0,9,0,0">
                                        <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="•" Foreground="{DynamicResource Primary}"/><TextBlock Grid.Column="1" Text="{Binding}" Foreground="{DynamicResource InkSecondary}" TextWrapping="Wrap"/></Grid></DataTemplate></ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="2" Background="{DynamicResource SurfaceElevated}" BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,0" Padding="20,13">
            <Button Content="关闭" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" MinWidth="92" IsCancel="True"/>
        </Border>
    </Grid>
</Window>
