# AI Sales OS v1.18.3

## WhatsApp 已读气泡跨板块稳定修复

- 会话已读游标改为只向前推进，后台同步不能再用较早快照覆盖已读状态。
- 同一数据库的 WhatsApp 会话写入增加串行保护，消除已读操作与后台同步并发保存时恢复旧未读数的竞态。
- 晚到但时间早于已读游标的消息按历史消息处理，不再重新产生新消息气泡。
- 增加“读完会话 → 切换板块 → 后台同步 → 返回 Inbox”的自动回归测试。
