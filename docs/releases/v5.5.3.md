# AI Sales OS v5.5.3

## 本地端统一矢量导航图标

- 参考 PWA 的 Lucide 图标语义，为八个本地端模块重绘 WPF 原生矢量图标。
- 图标统一为 18×18 像素、2 像素设计线宽和 28 像素固定占位，导航文字整齐对齐。
- 图标继承导航按钮的语义前景色，自动适配亮暗主题以及普通、悬停、选中和禁用状态。
- 不再依赖大小和基线不一致的 Unicode 字符图标。

本版本继续保护本地 SQLite 客户数据、消息、账号、API 配置、知识库、自动化和报告；Windows 安装及自动更新继续创建或修复桌面快捷方式，macOS 发布保持暂停。
